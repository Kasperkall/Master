batch_size: 20
learning_rate: 0.0008
input_channels: 3 #RGB
epochs: 10
validation_cadence: 2 #How many training sets are gone through per validation set
loss_weights: [0.3,0.7] #For cross entropy loss weighting
avg_per_im: 5 #Not used

img_dir: images500x500/combine #scanv2/dataset/train-raw #alu_test_im #This is an example of 10 images, the image directory for the complete dataset is on harddrive
gt_dir: images500x500/gtCombine #scanv2/dataset/ground-truth  #alu_masks 
save_dir: results
validation_fraction: 0.2